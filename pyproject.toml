[project]
name = "infra-log-sentinel-agent"
version = "0.1.0"
description = "Infrastructure log sentinel agent for Claw-a-thon 2026"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
